interface graphicsSettings {
    fps:number
    resolution:number
}

interface audioSettings {
    master:number
    music:number
    effect:number
}

interface gameplaySettings {
    keybinds:Array<string>
    VFX:boolean
    fastLate:boolean
    vLine:boolean
}