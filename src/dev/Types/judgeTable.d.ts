interface judgeTable {
    stunningBloom: table
    bloom: table
    miss: table
}
interface table {
    key: string
    label: string
    range: number
    scoreMultiplier: number
    color:number
}
